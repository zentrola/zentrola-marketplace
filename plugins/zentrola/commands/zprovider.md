---
description: Query the current configured Zentrola service provider name
---

Use the `provider` skill to interpret the request below, call the Zentrola MCP `get_provider` tool with no arguments, and return only the configured service provider name. Respond in the user's current language, and never expose or ask the user to paste an endpoint or Access Key.

User request: $ARGUMENTS
