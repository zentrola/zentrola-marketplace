---
description: Query the current Zentrola user's token usage for this month
---

Use the `zusage` skill to call the Zentrola Usage MCP `get_usage` tool and return the current user's token usage for the current month and reporting period. Report only data actually returned by the tool, respond in the user's current language, and never ask the user to paste an endpoint or Access Key.
